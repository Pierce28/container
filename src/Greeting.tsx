import * as React from "react";
import styles from "./Greeting.module.css";

const Greeting = () => (
    <div className={styles.card}>Greetings from the container app!</div>
);

export default Greeting;